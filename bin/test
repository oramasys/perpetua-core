#!/usr/bin/env bash
# Thin dev wrapper — delegates to the test suite under src/tests.
# perpetua-core is a library kernel (no runtime CLI); this is its only executable.
set -euo pipefail
cd "$(dirname "$0")/.."
exec "${PYTHON:-python3}" -m pytest "$@"
